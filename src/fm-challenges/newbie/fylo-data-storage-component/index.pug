extends ../../../includes/layout

block variables
  - const title = "Fylo data storage component";

block fonts
  +googleFonts([ 'family=Raleway&display=swap&text=You%E2%80%99ve%20sdfyrtag', 'family=Raleway:wght@700&display=swap&text=815%20GB0LEFT' ])

block content
  //- article? + menu
  main.content
    header.control
      h1.hidden Fylo data storage widget
      img(src='logo.svg' alt='Fylo logo')
      menu.toolbar(type='toolbar')
        button.iconBtn.newFile(aria-label='Create new file')
        button.iconBtn.newDir(aria-label='Create new directory')
        button.iconBtn.upload(aria-label='Upload')
    .status
      .bg
      label.stats
        | You’ve used #{''}
        span 815 GB #{''}
        | of your storage
        .meter
          meter.hidden(value='815' min='0' max='1000' title='GB')
          .bar
            div
          .label 0 GB
          .label 1000 GB
      .badge
        div
          span 185
          | GB Left
